# APP 303 — Context & Memory Engineering

Companion lab for `genai-gurukul/modules/app-303-context-memory.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest app-303/tests -q
# after filling starter TODOs:
# GURUKUL_LAB=starter pytest app-303/tests -q
```

Offline only — `FakeSummarizer`, local `tiktoken`. No paid APIs.
