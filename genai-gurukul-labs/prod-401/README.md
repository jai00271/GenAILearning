# PROD 401 — Evaluation in Production

Companion lab for `genai-gurukul/modules/prod-401-eval-production.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest prod-401/tests -q
# after filling starter TODOs:
# GURUKUL_LAB=starter pytest prod-401/tests -q
```

Offline only — `FakeJudge`, trajectory scorer, regression gate. No paid APIs.
