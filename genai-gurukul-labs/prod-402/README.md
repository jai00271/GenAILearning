# PROD 402 — Fine-Tuning & PEFT

Companion lab for `genai-gurukul/modules/prod-402-fine-tuning.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest prod-402/tests -q
# GURUKUL_LAB=starter pytest prod-402/tests -q
```

Offline — decision function + toy LoRA math (NumPy). No model downloads.
Decision-lab memo rubric: see `decision-lab/README.md` and the lesson page.
