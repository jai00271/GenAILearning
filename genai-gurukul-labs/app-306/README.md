# APP 306 — Vector Databases & Semantic Search

Companion lab for `genai-gurukul/modules/app-306-vector-databases.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest app-306/tests -q
# after filling starter TODOs:
# GURUKUL_LAB=starter pytest app-306/tests -q
```

Offline only — NumPy in-memory cosine index (FAISS optional in prod; not required here).
No Pinecone / OpenSearch / paid APIs.
