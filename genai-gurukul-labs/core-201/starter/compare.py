"""CORE 201 — compare token footprints across encodings (starter)."""

from __future__ import annotations

from counting import count_tokens


def compare_encodings(text: str, names: list[str]) -> dict[str, int]:
    """Return {encoding_name: token_count} for each name in order given."""
    # TODO
    raise NotImplementedError
