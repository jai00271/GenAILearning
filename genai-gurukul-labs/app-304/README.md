# APP 304 — Evaluation Fundamentals

Companion lab for `genai-gurukul/modules/app-304-evaluation-fundamentals.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest app-304/tests -q
# after filling starter TODOs:
# GURUKUL_LAB=starter pytest app-304/tests -q
```

Offline only — NumPy metrics + JSONL goldens. No paid APIs.

Known-good copies also live under `checkpoints/app-304/` for APP 305+ imports.
