# PROD 403 — Guardrails, Safety & Responsible AI

Companion lab for `genai-gurukul/modules/prod-403-guardrails.html`.

```bash
cd genai-gurukul-labs
source .venv/bin/activate
GURUKUL_LAB=solution pytest prod-403/tests -q
# GURUKUL_LAB=starter pytest prod-403/tests -q
```

Offline — `detect_indirect_injection` on retrieved chunks + PII redaction citation-leak demo.
